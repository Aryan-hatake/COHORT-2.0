GENERATIVE AI  = ai/tool that generates a new content [text,video,image,audio]

ASP - is ai service provider that provides api keys or access to their ai /llm model

AI - is a tool that takes input and gives output, it is stateless hence no memory

server provides memory to ai during conversation

langchain helps server in communicating with AI model