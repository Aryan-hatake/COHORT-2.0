Redux is used for data centralization 

Redux is used over context in large scale applications because each feature has its own data which results into creating its own context if an app has atleast 100 feature 100 context will be created which will be hard to manage 

hence redux is used for data centralization where data of each and EVERY FEATURE IS STORED