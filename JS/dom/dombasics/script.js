//dom - 4 pillars;

// 1. selection of html Tag

     // - document.querySelector();  [{selects first element},{ tag, id, class}]
     // - document.getElementById();
     // - document.querySelectorAll();

// 2. changing the content of html tag

     // - textContent
     // - innerHTML
     
// 3. changing the css of html tag
// 4. applying the eventlistner on html tag